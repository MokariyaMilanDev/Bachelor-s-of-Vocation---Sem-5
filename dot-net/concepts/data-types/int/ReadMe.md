# Int

#### Properties

<!-- ##### `.Length`

```cs
<Int>.Length
``` -->

#### Methods

##### `.ToString()`

```cs
<Int>.ToString()
```

##### `.ToString()`

- Currency

```cs
<Int>.ToString("C")
```

##### `.ToString()`

- Commas

```cs
<Int>.ToString("N")
```

- Hexadecimal

##### `.ToString()`

```cs
<Int>.ToString("X")
```

##### `.GetType()`

```cs
<Int>.GetType()
```

##### `.CompareTo()`

```cs
<Int>.CompareTo(int Number)
```

##### `.Equals()`

```cs
<Int>.Equals(int Number)
```

---

##### `int.Parse()`

```cs
int.Parse(String separator, String[] value)
```

---

##### `Convert.ToString()`

```cs
Convert.ToString(int Number, base Number)
```

---

##### `Math.Sign()`

```cs
Math.Sign(int Number)
```

##### `Math.Abs()`

```cs
Math.Abs(int Number)
```

##### `Math.Max()`

```cs
Math.Max(x Number, y Number)
```

##### `Math.Min()`

```cs
Math.Min(x Number, y Number)
```

##### `Math.Pow()`

```cs
Math.Pow(base Number, exponent Number)
```

##### `Math.Sqrt()`

```cs
Math.Sqrt(int Number)
```

##### `Math.Round()`

```cs
Math.Round(x Double)
```

##### `Math.Ceiling()`

```cs
Math.Ceiling(x Double)
```

##### `Math.Floor()`

```cs
Math.Floor(x Double)
```

##### `Math.Truncate()`

```cs
Math.Truncate(x Double)
```
